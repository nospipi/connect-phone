// create a resource with module,service,specs etc
nest g resource resources/<name>

# Generate migration file
npx typeorm-ts-node-commonjs migration:generate src/database/migrations/Migration -d src/database/data-source.ts

# Run all pending migrations
npx typeorm-ts-node-commonjs migration:run -d src/database/data-source.ts

# Run seed file
npm run db:seed 
npm run db:seed:countries
npm run db:seed:prices


______________________

# Revert the migration
npx typeorm-ts-node-commonjs migration:revert -d src/database/data-source.ts

# Then run it again
npx typeorm-ts-node-commonjs migration:run -d src/database/data-source.ts

_______________________
# Tests

npx jest -t "test_name"
npx jest -t "test_name1 | test_name2"
 npx jest "src/resources/date-ranges/.*"

_________________________________________________________________________________________
# Custom create resource
npm run create-service "users" "test-service"  


---- 

in apps/api/src/

1. go through all files in all folders and replace all the import paths from relative imports like for example import { OrganizationGuard } from '../../../../../common/guards/organization.guard' to aliased imports like for example --> import { OrganizationGuard } from '@/common/guards/organization.guard';

* do it manually one by one, dont create and run any scripts

---- 

in apps/api/src/

1. go through all files in all folders and check at the top of the file, i have in each file a comment with the file path lime for example : // apps/api/src/resources/cms/user-invitations/services/get-all-invitations-of-org-paginated/controller.ts, make sure this comment represents the correct file path of its file, for instance most of these comments
are outdated and the miss the /cms like you may find // apps/api/src/resources/user-invitations/services/get-all-invitations-of-org-paginated/controller.ts and not the correct // apps/api/src/resources/cms/user-invitations/services/get-all-invitations-of-org-paginated/controller.ts
, nevertheless, you have to make sure the file path is correct

* do it manually one by one, dont create and run any scripts